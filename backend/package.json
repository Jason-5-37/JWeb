{
  "name": "backend",
  "version": "1.0.0",
  "description": "This is the backend for Jason person website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm start --prefix client",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "start": "node app.js"
  },
  "author": "Zhanhong Lu",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.2",
    "express": "^4.18.1"
  }
}
